1.self的两个特点：
*类下面的实例方法，参数固定放一个self。
*调用实例方法的时候，不需要self。

2.其他语言用this代替self。
和其他语言的区别是self在参数中显示出来。可能是因为Python的特点-显胜于隐。
Python也可以用改成this，但是Python建议我们用self。

3.self的意义：
self就是调用某一方法的对象。
    def do_homework(self): 
        print('homework')
        
student1 = Student('石敢当', 18)
student1.do_homework() # do_homework(self)的self指的就是student1。
self和对象有关，和类没有关系。谁调用了这个方法，self指代的就是谁。话句话说，self代表的是实例，而不是类。

4.实例方法:实例可以调用的方法。
最大的特点：第一个参数需要传入self。

5.self可以改成其他的名字，所以不是关键字。
