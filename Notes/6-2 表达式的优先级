1.优先级
  1 ** 
  2 ~ + - 
  3 * / % //
  4 + -
  5 >> << 
  6 &
  7 ^ |
  8 <= < > >= 比较运算符
  9 <> == 1= 等于运算符
  10 = %= /m //m -= += *= **= 赋值运算符 
  11 is is not 身份运算符
  12 in not in 成员运算符
  13 not and or 逻辑运算符 （and优先级高于or）
2.编辑器默认从左向右解析（左结合），先执行优先级高的运算符。可以用()强制增加优先级。括号的优先级最高。
  特例：赋值运算符。
  a=1
  b=2
  c=a or b  右结合，先执行等号右边，再执行赋值运算
3.a=1
  b=2
  c=2
  (not a) or ((b + 2) == c)
  False
 
