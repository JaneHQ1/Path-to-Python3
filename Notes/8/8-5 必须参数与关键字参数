1.必须参数：
def add(x,y)
    result = x + y
    return result
x,y是必须参数，必须给他们赋值。
x,y是形式参数。
a = add(1,2)
调用函数时的1,2是实际参数。
2.关键字参数：
不用考虑参数的顺序。
c = add(y=3, x=2)
调用函数的时候，明确告知实参是给哪个形参赋值。
意义：
*提高代码的可读性
damage(skill1=1, skill2=2)
*不需要强制记忆参数的顺序。
3.关键字参数和必须参数的区别在于函数的调用，它们的定义相同。
定义多少个形参，就要传递多少个时参。
