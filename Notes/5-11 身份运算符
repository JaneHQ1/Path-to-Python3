1.身份运算符
  is
  is not
2.身份运算符与对象有关。是不是和Java中的is类似？
3.返回的结果是bool。
4.如果两个变量取值相等，则is返回True。
  似乎和==没有区别，但是：
  a==1
  b==1.0
  a==b
  True 
  a is b
  False 
  == 比较两个值是否相等，不关心内存地址。
  is 比较的是两个变量的内存地址是否相等。
  因为内存地址不同，所以身份不同。 
  课后练习：
  a={1,2,3}
  b={2,1,3}
  a==b
  Ture {set}是无序的
  a is b
  False 内存地址不同 
  a={1,2,3}
  b={2,1,3}
  a==b
  False (tuple)是有序的
  a is b
  False
