1.将type当作变量名：
  type=1
  type(1) 
  会报错，因为type已经被赋值1，这就等于1(1),是没有意义的。
2.a=1
  b=a
  a=3
  print(b) b不会改变
  a=[1,2,3,4,5]
  b=a
  a[0]='1'
  print(b) b改变了why?
  值类型:int,str,tuple
  引用类型:list,set,dict
  值类型不可变，引用类型可变。
  字符串不可变，为什么可以增加字符串？
  a='hello'
  print(id(a))
  2277377864512
  a=a+'python'
  print(id(a))
  2277380468400
  两个a的内存地址不一样，所以两个a实际上不是一个字符串。
