1.Python解析与其他语言的不同：
  *每一行语句末尾不强制加;
  *不需要{}把流程包裹起来
2.Python靠缩进区分代码段。
3.流程控制语句：
  *条件控制：if else
  *循环控制: for while 
  *分支 switch (Python 里没有switch语法，不代表无法实现分支)
4.条件控制：
  mood=True
  if mood:
      print('Go to left')
      #前面有四个空格。
  else:
      print('Go to right')
5.Python代码无法混淆和压缩。
