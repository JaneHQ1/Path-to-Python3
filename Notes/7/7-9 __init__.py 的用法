1.一次导入多个变量：
\代码换行（太丑，不推荐）：
from c9 import a,b,\
c
print(a)
推荐用括号换行：
from c9 import (a,b,
c)
2.当一个包被导入，init文件首先被执行。
3.通常会在init文件里做包和模块初始化工作。
4.无论是导入包或者模块下的变量，Python都会自动运行init文件。
5.同样在init里可以选定哪些模块被导出。
__all__ = ['c7']
6.在init文件中批量导入，就不用在模块里重复导入。
